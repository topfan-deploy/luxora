services:
  postgres:
    image: postgres:16-alpine
    container_name: luxora-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: luxora
    ports:
      - "5433:5432"
    volumes:
      - luxora_pgdata:/var/lib/postgresql/data

volumes:
  luxora_pgdata:
